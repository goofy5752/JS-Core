function solve(input) {
    let numLength = 
    Number(String.prototype.split(', ', input)[0]);
    let numToAdd = 
    Number(String.prototype.split(', ', input)[1]);

    for (let i = 0; i < numLength; i++) {
        const element = numLength;
        
    }
}