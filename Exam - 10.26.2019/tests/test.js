const { expect } = require('chai');

describe('test', function() {
    describe('first group', function() {
        if ('test', function() {
                expect(0 === 0).to.be.true;
            });
    });
});