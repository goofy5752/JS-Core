const { expect } = require('chai');

describe('test', function() {
    describe('first group', function() {
        it('test', function() {
            expect(0).to.equal(0);
        });
    });
});

//const hire = () => store.hire('name', 'position')
//expect(hire).to.throw(Error, 'This .....................')